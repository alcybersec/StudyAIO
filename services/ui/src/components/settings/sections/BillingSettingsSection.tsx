import { BillingSection } from '../../billing/BillingSection'

/** Settings section wrapper mounting the existing plan & billing UI. */
export function BillingSettingsSection() {
  return <BillingSection />
}
